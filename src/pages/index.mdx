---
title: "Hello!"
tags: [profile,site,no-show]
summary: "I'm Max! This is a (small) collection of various projects I've worked on over the years. "
---

import { graphql } from "gatsby"
import Project from "../components/project"
import ProjectList from "../components/project-list"
import ProjectTabs from "../components/project-tabs"

I'm Max! This is a collection of various projects I've worked on over the years.


<div class="project-tabs-sticky"><h2 id="featured">Featured Projects</h2></div>
<ProjectList showImg list={props.data.allMdx.edges} filter="featured" />

<ProjectTabs by="Type" data={props.data.allMdx.edges} tabs={["web", "game", ["cli", "CLI"], , "collaborative", "joke"]} />

<ProjectTabs by="Language" data={props.data.allMdx.edges} tabs={[["js", "JS"], "java", "python", "rust"]} />

<ProjectTabs by="Library" data={props.data.allMdx.edges} tabs={["vue", "react", ["tensorflow", "TensorFlow"], "electron"]} />

<ProjectTabs by="DB" data={props.data.allMdx.edges} tabs={["firestore", ["mongodb", "MongoDB"], "s3", "neo4j"]} />

<ProjectTabs by="Cloud Platform" data={props.data.allMdx.edges} tabs={["firebase", ["gcp", "GCP"], ["aws", "AWS"], "heroku"]} />


export const pageQuery = graphql`
query {
allMdx {
edges {
node {
id
frontmatter {
tags
title
summary
img
}
slug
}
}
}
}
`
