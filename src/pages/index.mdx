---
title: "Hello!"
tags: [profile,site,no-show]
summary: "I'm Max! This is a (small) collection of various projects I've worked on over the years. "
---

import { graphql } from "gatsby"
import Project from "../components/project"
import ProjectList from "../components/project-list"
import ProjectTabs from "../components/project-tabs"
import { Tab, Tabs, TabList, TabPanel, resetIdCounter } from 'react-tabs';
import 'react-tabs/style/react-tabs.css';
resetIdCounter();


I'm Max! This is a collection of various projects I've worked on over the years.


## Featured Projects
<ProjectList showImg list={props.data.allMdx.edges} filter="featured" />

## Projects by Type
<ProjectTabs data={props.data.allMdx.edges} tabs={["web", "game", ["cli", "CLI"], , "collaborative", "joke"]} />

## Projects by Language

<ProjectTabs data={props.data.allMdx.edges} tabs={[["js", "JS"], "java", "python", "rust"]} />

## Projects by Library

<ProjectTabs data={props.data.allMdx.edges} tabs={["vue", "react", ["tensorflow", "TensorFlow"], "electron"]} />

## Projects by DB

<ProjectTabs data={props.data.allMdx.edges} tabs={["firestore", ["mongodb", "MongoDB"], "s3", "neo4j"]} />

## Projects by Cloud Platform

<ProjectTabs data={props.data.allMdx.edges} tabs={["firebase", ["gcp", "GCP"], ["aws", "AWS"], "heroku"]} />


export const pageQuery = graphql`
query {
allMdx {
edges {
node {
id
frontmatter {
tags
title
summary
img
}
slug
}
}
}
}
`
